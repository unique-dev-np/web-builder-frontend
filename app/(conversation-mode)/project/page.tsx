import ChatInputBox from "@/components/ChatInputBox";

function ProjectPage() {
  return (
    <section className="w-[80vw] h-screen relative">
      <ChatInputBox />
    </section>
  );
}

export default ProjectPage;
